import { reset{{pascalCase name}}State, set{{pascalCase name}}State } from './{{kebabCase name}}.state'; 
export const initialize{{pascalCase name}} = async () => { try { set{{pascalCase name}}State({ loading: true }); 

set{{pascalCase name}}State({ loading: false, initialized: true, error: null }); return true; }
catch (error) { set{{pascalCase name}}State({ loading: false, error: error instanceof Error ? error.message : 'An
unknown error occurred' }); return false; } };

export const clear{{pascalCase name}} = () => { reset{{pascalCase name}}State(); };